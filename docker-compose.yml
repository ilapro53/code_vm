services:
  ai-tool:
    build:
      context: .
      args:
        AGENT: mimo
    volumes:
      - ./mnt:/workspace/mnt:rw
      - ./input:/workspace/input:ro
      - E:\:/host_mnt/e:rw
      - T:\:/host_mnt/t:rw
      - O:\:/host_mnt/o:rw
    cap_add:
      - SYS_ADMIN
    stdin_open: true
    tty: true
